# Kodosumi service configuration for Aikido Audit Reviewer
name: aikido-reviewer
route_prefix: /aikido-reviewer
import_path: agents.aikido_reviewer.kodosumi_app:app
runtime_env:
  working_dir: .
  pip:
    - fastapi>=0.104.0
    - uvicorn[standard]>=0.24.0
    - pydantic>=2.0.0
    - python-dotenv
    - anthropic
    - httpx
    - kodosumi
